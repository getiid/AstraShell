# AstraShell v0.3.20

## 本次更新

- 重新编写 `README.md`，把当前桌面端主线、核心模块、工作流、同步策略和开发命令对齐到现在的软件状态。
- 项目展示页同步替换主工作台图片，新的展示图覆盖主机、SSH、SFTP、本地终端、片段、数据库、Vault 和日志等核心面板。
- 桌面端版本号提升到 `0.3.20`，为重新打包和发布新一轮安装资产做准备。
- 补齐当前版本的发布说明，避免对外文档停留在旧版本描述。

## 当前发布资产

- mac 自动升级包：`AstraShell-0.3.20-mac.zip`
- Linux 包：`AstraShell-0.3.20-arm64.AppImage`
- Windows 安装包：`AstraShell-Setup-0.3.20.exe`

## 自动升级说明

- Windows：通过 GitHub Release + `latest.yml` 自动升级。
- macOS：通过 GitHub Release + `latest-mac.yml` 检查新版；当前公开构建仍是未签名包，下载后可能需要手动执行去隔离命令。
- Linux：通过 `latest-linux-arm64.yml` 提供更新元数据。
