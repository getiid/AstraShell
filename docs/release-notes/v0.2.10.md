## 本次更新（七牛更新源接入）

### 自动更新
- 新增七牛更新源支持，客户端检查顺序调整为：`七牛 -> GitHub -> Gitee`。
- 自动更新请求对元数据启用防缓存参数，降低 `latest.yml` 被缓存导致的漏更新风险。

### 发布流程
- 新增七牛发版脚本，可自动上传 `latest*.yml`、安装包和 blockmap。
- 新增一键发版脚本，支持本地环境文件复用，减少手工操作。

### 说明
- 当前 `astra.jitux.com` 可直接作为更新源访问。
- 若域名不是七牛 CDN 加速域名，发布脚本会跳过刷新接口，不影响正常上传和更新检查。
